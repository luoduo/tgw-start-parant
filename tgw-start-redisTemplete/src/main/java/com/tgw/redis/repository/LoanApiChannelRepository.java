package com.tgw.redis.repository;

import com.tgw.redis.model.LoanApiChannel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LoanApiChannelRepository extends JpaRepository<LoanApiChannel, Integer> {
}